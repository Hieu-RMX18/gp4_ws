[develop]
script_dir=$base/lib/gp4_perception
[install]
install_scripts=$base/lib/gp4_perception

[tool:pytest]
testpaths = test
addopts = -rs
markers =
    ros_integration: requires colcon-sourced workspace with built interfaces package
