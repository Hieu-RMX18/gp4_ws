[develop]
script_dir=$base/lib/llm_gateway
[install]
install_scripts=$base/lib/llm_gateway

[tool:pytest]
testpaths = tests
addopts = -rs
markers =
    ros_integration: requires colcon-sourced workspace with built interfaces package
